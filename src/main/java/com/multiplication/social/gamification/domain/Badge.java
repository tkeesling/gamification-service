package com.multiplication.social.gamification.domain;

public enum Badge {

    // dependent on score
    BRONZE_MULTIPLICATOR,
    SILVER_MULTIPLICATOR,
    GOLD_MULTIPLICATOR,

    // other conditions applied
    FIRST_ATTEMPT,
    FIRST_WIN
}
